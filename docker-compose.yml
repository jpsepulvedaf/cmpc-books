# ─────────────────────────────────────────────────────────────────────────────
# CMPC Libros — full stack local orchestration
#   up:   docker compose up --build
#   down: docker compose down -v   (removes volumes)
# ─────────────────────────────────────────────────────────────────────────────

name: cmpc-books

services:
  db:
    image: postgres:16-alpine
    container_name: cmpc-db
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-cmpc}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-cmpc}
      POSTGRES_DB: ${POSTGRES_DB:-cmpc_books}
    ports:
      # Published on the host at ${DB_HOST_PORT} (default 5433) to avoid
      # clashing with a local PostgreSQL on 5432. Configurable via .env:
      # set DB_HOST_PORT and DATABASE_URL to the same value when customizing.
      - '${DB_HOST_PORT:-5433}:5432'
    volumes:
      - db-data:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U ${POSTGRES_USER:-cmpc} -d ${POSTGRES_DB:-cmpc_books}']
      interval: 5s
      timeout: 5s
      retries: 10

  api:
    build:
      context: ./apps/api
      target: dev
    container_name: cmpc-api
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
    environment:
      NODE_ENV: ${NODE_ENV:-development}
      API_PORT: 3000
      DATABASE_URL: postgresql://${POSTGRES_USER:-cmpc}:${POSTGRES_PASSWORD:-cmpc}@db:5432/${POSTGRES_DB:-cmpc_books}?schema=public
      JWT_SECRET: ${JWT_SECRET:-dev-secret-only}
      JWT_EXPIRES_IN: ${JWT_EXPIRES_IN:-12h}
      SWAGGER_ENABLED: ${SWAGGER_ENABLED:-true}
    ports:
      - '3000:3000'
    volumes:
      - ./apps/api:/usr/src/app
      - /usr/src/app/node_modules
    command: sh -c "npx prisma generate && npx prisma migrate deploy && npm run seed && npm run start:dev"

  web:
    build:
      context: ./apps/web
      target: dev
    container_name: cmpc-web
    restart: unless-stopped
    depends_on:
      - api
    environment:
      # Used by vite.config.ts to proxy /api and /uploads inside the compose network
      VITE_API_PROXY_TARGET: http://api:3000
      # Used by the browser to resolve book cover images (host port of the API)
      VITE_MEDIA_ORIGIN: http://localhost:3000
    ports:
      - '5173:5173'
    volumes:
      - ./apps/web:/usr/src/app
      - /usr/src/app/node_modules
    command: npm run dev

volumes:
  db-data: